#!/bin/sh
/bin/echo 1 > /proc/sys/net/ipv4/conf/eth0/proxy_arp

IP="<%= @virtual_ipaddress %>"
for ip in $IP ; do
  /sbin/ip route add $ip dev lo
#  /bin/echo "garp -i eth0 -a $ip -n 10" | logger -t GARP
#  /usr/bin/garp -i eth0 -a $ip -n 10
done